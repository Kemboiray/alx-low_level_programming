#include "main.h"

/**
 * mul - multiply 2 integers
 * @a, @b: integer variables being multiplied
 * Return: multiple (success)
 */

int mul(int a, int b)
{
	int multiple;

	multiple = a * b;
	return (multiple);
}
