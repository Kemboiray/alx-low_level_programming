#include "main.h"

/**
 * mul - multiply 2 integers
 * @a: integer variabke
 * @b: another integer variabke
 * Return: multiple (success)
 */

int mul(int a, int b)
{
	int multiple;

	multiple = a * b;
	return (multiple);
}
