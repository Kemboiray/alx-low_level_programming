#include "main.h"

/**
 * _strcmp - compare two strings
 * @s1: pointer to first string
 * @s2: pointer to second string
 *
 * Return: -ve integer, 0 or +ve integer if respectively, s1 <, == or > s2
 */

int _strcmp(char *s1, char *s2)
{
	int retval = s1 - s2;

	return (retval);
}
