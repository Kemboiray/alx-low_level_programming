#include "main.h"

/**
 * clear_bit - sets the value of a bit to 0 at a given index.
 * @n: pointer to number string.
 * @index: index of bit.
 *
 * Return: 1 (success), or -1 (failure).
*/
int clear_bit(unsigned long int *n, unsigned int index)
{
	;
}
